# Fichier de configuration Docker pour DHIS2 Sync
# Copier ce fichier vers .env et ajuster les valeurs

# ===== DJANGO SETTINGS =====
SECRET_KEY=+pxcc!_8yr^y=hsaqx%7k^e*gw6&p94-fr!ici7fy@gcd5d-gp
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# ===== DATABASE SETTINGS (PostgreSQL) =====
POSTGRES_DB=dhis2sync
POSTGRES_USER=dhis2user
POSTGRES_PASSWORD=SecurePassword123!
POSTGRES_HOST=db
POSTGRES_PORT=5432

# ===== REDIS SETTINGS =====
REDIS_HOST=redis
REDIS_PORT=6379

# ===== CELERY SETTINGS =====
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0
CELERY_ONCE_REDIS_URL=redis://redis:6379/0

# ===== SUPERUSER (créé automatiquement au premier démarrage) =====
# Laisser vide pour ne pas créer de superutilisateur
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=ChangeThisPassword123!

# ===== DHIS2 SETTINGS =====
VERIFY_SSL=true
DEFAULT_ORG_UNITS=
DEFAULT_DATASETS=
DEFAULT_PROGRAMS=
DEFAULT_START_DATE=2025-01-01
DEFAULT_END_DATE=2025-12-31
AUTOSYNC_PROBE_EVERY_MIN=2

# ===== EMAIL SETTINGS =====
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.example.org
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=true
FROM_EMAIL=dhis2-sync@example.com
REPORT_TO_EMAILS=ops@example.com,admin@example.com
REPORT_AT_HOUR=6
REPORT_AT_MIN=0

# ===== SECURITY SETTINGS (pour production avec HTTPS) =====
# Décommenter quand HTTPS est configuré
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
# SECURE_HSTS_SECONDS=31536000
